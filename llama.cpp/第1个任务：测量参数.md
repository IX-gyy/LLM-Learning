# 在开发板上使用llama.cpp和vllm运行同一模型，通过bench server命令得到相关策略结果
## 1.准备环境
```
# 1. 更新系统
sudo apt update
sudo apt upgrade -y
sudo reboot

# 2. 设置最大功率模式（必须！）
sudo nvpmodel -m 0
sudo jetson_clocks
sudo reboot
```
并记录系统版本，后续下载docker要匹配
```
cat /etc/nv_tegra_release
```

## 2.安装docker
此处使用清华镜像源：
```
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io
```
安装NVIDIA Toolkit
```
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | sudo tee /etc/apt/sources.list.d/nvidia-docker.list
sudo apt update
sudo apt install -y nvidia-container-toolkit
sudo systemctl restart docker
```
验证，如果正常输出GPU信息，则准备就绪`docker run --rm --runtime=nvidia nvcr.io/nvidia/l4t-base:r36.4.0 nvidia-smi`

## 3.模型准备
主要模型：
- `Qwen/Qwen3-VL-2B-Instruct`
- `Qwen/Qwen3-VL-8B-Instruct`
- `cyankiwi/Qwen3-VL-2B-Instruct-AWQ-4bit`
- `cyankiwi/Qwen3-VL-8B-Instruct-AWQ-4bit`

## 4.拉取docker镜像
根据之前查到的系统类型，选择我们需要的docker镜像，这里我是`# R35 (release), REVISION: 5.0, GCID: 35550185, BOARD: t186ref, EABI: aarch64, DATE: Tue Feb 20 04:46:31 UTC 2024`，  
所以我们选择：  
vllm镜像（先看看能否跑通，再试试llama）
```
```
启动vllm服务容器：
创建容器并挂载模型目录（注意将 /home/tp/Workspace 替换为你实际的路径，如 /home/yourname/Workspace）
```
docker run -itd \
  --name vllm_deploy \
  --runtime nvidia \
  --network host \
  --ipc host \
  -e NVIDIA_VISIBLE_DEVICES=all \
  -e NVIDIA_DRIVER_CAPABILITIES=compute,utility \
  -v /home/yourname/Workspace:/workspace \
  ghcr.io/nvidia-ai-iot/vllm:r36.4-tegra-aarch64-cu126-22.04
```

## 5.进入容器并启动模型服务
进入容器：
```
docker exec -it vllm_deploy bash
```
激活虚拟环境（镜像内置）：
```
source /opt/venv/bin/activate
```
启动vllm服务：
```
vllm serve /workspace/models/Qwen3-VL-8B-Instruct-AWQ-4bit \
  --max-model-len 4096 \
  --gpu-memory-utilization 0.75 \
  --port 8000
```
再开一个终端测试是否成功启动：`curl http://127.0.0.1:8000/health`，如果返回`{"status":"ok"}`则正常

## 6.开始测试！
终于开始测试了捏，参考下面的命令行
```
vllm bench serve \
  --backend openai-chat \
  --model /workspace/models/Qwen3-VL-8B-Instruct-AWQ-4bit \
  --host 127.0.0.1 \
  --port 8000 \
  --endpoint /v1/chat/completions \
  --dataset-name hf \
  --dataset-path lmarena-ai/vision-arena-bench-v0.1 \
  --hf-split train \
  --num-prompts 128 \
  --request-rate inf \
  --max-concurrency 1
```
--backend openai-chat：使用 OpenAI 兼容的聊天接口。
--model：指定模型路径（仅用于日志，不影响服务）。
--host 和 --port 指向正在运行的服务。
--dataset-name hf：从 Hugging Face 加载数据集。
--dataset-path lmarena-ai/vision-arena-bench-v0.1：这是一个视觉问答数据集，包含图片和问题。
--num-prompts 128：发送 128 条请求。
--request-rate inf：无限请求率，即尽可能快地发送（压力测试）。
--max-concurrency 1：最大并发数为 1，即一次只处理一个请求。
